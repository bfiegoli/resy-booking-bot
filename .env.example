# Required
RESY_API_KEY=VbWk7s3L4KiK5fzlO7JD3Q5EYolJI7n5
ENCRYPTION_KEY=          # 64-char hex string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Optional
SITE_PASSWORD=           # Password-protect the UI (leave blank to disable)
CRON_SECRET=             # Bearer token for /api/cron/snipe endpoint
DB_PATH=./resy-sniper.db # SQLite database location
